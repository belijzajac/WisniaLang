# The tests are not build by default. To build them, set the
# unittests option to ON. You can do it by specifying the
# -Dunittests=ON flag when running cmake.
option(unittests "Build all of wisnia's unit tests." OFF)
